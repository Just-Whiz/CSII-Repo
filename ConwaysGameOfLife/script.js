const BtnAdd = document.querySelector(".btn-add");

BtnAdd.addEventListener.addEventListener("click", AddNew)

function Addnew() {
    const newDiv = document.createElement("div")
    console.log("add")
    document.body.appendChild(newDiv)
}